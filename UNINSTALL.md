# Uninstall

```bash
curl -fsSL https://raw.githubusercontent.com/nightnei/wordpress-developer-mcp-server/main/uninstall.sh | bash
```

It removes `~/.wordpress-developer-mcp` and MCP entries from supported AI apps.
Sites under `~/Studio` stay on disk unless you delete them yourself.
